#include "main.h"

/**
 * _abs - program to print absolut value of integer
 * @n: the integer to be computed
 * Return: Absolute value of integer
*/

int _abs(int n)
{
	if (n >= 0)
		return (n);
	else
		return (-n);
}
